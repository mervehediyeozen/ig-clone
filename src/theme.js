import { extendTheme } from "@chakra-ui/react";

// Chakra UI temasını özelleştiriyoruz
export const theme = extendTheme({
  colors: { 
    borders: "#e3ddcf",   // 'borders' rengi
    deneme: "#b89953",     // 'deneme' rengi
  },
});

